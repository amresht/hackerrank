#!/home/amresh/anaconda3/bin/python

#read a strong of numbers separated by space
print("Enter a: ", end='')

list = list(map(int, input().split()))

z = max(list)
print("Max now is {}". format(z))

while() max(list) == z):
    (list.remove(max(list))

print("Max now is "+ str(max(list)))